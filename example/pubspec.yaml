name: permission_geo_handler_example
description: Demonstrates how to use the permission_geo_handler plugin.

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  permission_geo_handler:
    path: ../

flutter:
  uses-material-design: true